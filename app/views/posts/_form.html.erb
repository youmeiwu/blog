<%= simple_form_for @post do |f| %>
  <%= f.input :title %>
  <%= f.input :body %>
  <% if @post.present? %>
    <%= f.submit "Update Post", data: { disable_with: "Submitting..." } %>
  <% else %>
    <%= f.submit "Create Post", data: { disable_with: "Submitting..." } %>
  <% end %>
<% end %>
